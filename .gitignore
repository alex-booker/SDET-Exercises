node_modules/
test-results/

# Specs generados por playwright-bdd a partir de features/*.feature.
# No se versionan: se regeneran en cada `npm run test:bdd` (ver playwright.config.js).
.features-gen/

# Screenshot de evidencia generado por mobile/launch-sample-app.js.
mobile/last-screenshot.png

# Screenshots/page source/logcat/video generados por mobile/run-suite.js.
# Se regeneran en cada corrida; en CI se suben como artifacts de GitHub Actions
# en vez de versionarse (ver .github/workflows/ci.yml).
mobile/artifacts/
