# SDET-Exercises
This Repository contains the SDET exercises and future deliverables for this course

26/06/2026 - Adding the "01 - Front-end Automation" exercise

29/07/2026 - Adding a Design Patterns demo (Singleton, Page Factory, Screenplay) on top of the existing Page Object Model. See [PATTERNS.md](./PATTERNS.md).

30/07/2026 - Refactored the flat Page Object Model into a layered POM: BasePage, chained page objects (LoginPage -> InventoryPage -> CartPage), and a ProductItemComponent shared between the catalog and the cart. See [PATTERNS.md](./PATTERNS.md).

03/08/2026 - Added a BDD deliverable: `features/login.feature` (3 Gherkin scenarios covering the login flow) with step definitions in `steps/login.steps.js`, wired up via playwright-bdd on top of the existing `LoginPage`. Run with `npm run test:bdd`.

11/08/2026 - Added a Mobile Automation deliverable under `mobile/` (Appium + Android emulator): `launch-sample-app.js` starts a session via capabilities and screenshots it, `find-package-name.js` automates discovering an app's package/activity via `adb logcat`, and `ACCESSIBILITY-IDS.md` proposes a stable accessibility-id contract with the dev team based on locator/gesture issues found while exploring with Appium Inspector. Run with `npm run mobile:launch` / `npm run mobile:find-package`.

12/08/2026 - Added a mobile Screen Object Model deliverable under `mobile/screens/` (webdriverio + Appium), mirroring the web POM already documented in `PATTERNS.md`: `BaseScreen` (common locators + a `scrollUntilVisible(label)` helper), `AlarmScreen` (with a platform branch between Android/iOS for the alarm toggle), and `SettingsScreen`. `mobile/tests/alarm-flat.js` and `alarm-screen-object.js` do the exact same thing (enable the first alarm) so they can be compared side by side — before/after the refactor. Run with `npm run mobile:alarm-flat` / `mobile:alarm-screen-object` / `mobile:settings-scroll-demo`.

13/08/2026 - Added failure diagnostics for the mobile suite: `mobile/support/captureDiagnostics.js` (screenshot + page source + logcat, saved under `mobile/artifacts/<test-name>/` whenever a test fails) and `mobile/support/recordScreen.js` (records video for the whole run via Appium's `startRecordingScreen`/`stopRecordingScreen`). `mobile/run-suite.js` ties both together over a small test list that includes one deliberate failure, to prove the mechanism actually fires. `ci.yml` gained a `mobile-test` job (Android emulator via `reactivecircus/android-emulator-runner`) that runs the suite and uploads `mobile/artifacts/` as a build artifact regardless of pass/fail — configured and documented, not yet exercised against a real PR. Run with `npm run mobile:run-suite`.

13/08/2026 - Added the Mobile Automation module's final deliverable, `mobile/final-exercise.js`: 3 real scenarios (enable an alarm, navigate to Settings > Accessibility, start a 1-minute timer) each through a Screen Object — including a new `TimerScreen` added just for this exercise, proving the Screen Object pattern generalizes to a screen none of the earlier deliverables touched. Reuses `captureDiagnostics()` for screenshot + page source on failure. Caught and fixed a real flaky-click bug in `BaseScreen.scrollUntilVisible` along the way (a `RecyclerView` can still be settling right after a scroll gesture completes; a bare click right after finding the element sometimes missed). Run with `npm run mobile:final-exercise`. See [mobile/README.md](./mobile/README.md) for the full setup/run instructions for the whole Mobile Automation module.

18/08/2026 - "LLM" topic exercise: asked an LLM (blind, no live inspection) to write `tests/03-llm-generated-sort.spec.ts` for the product sort dropdown. All 5 standard_user assertions passed unverified; 2 problem_user assertions failed for real (sort has no effect, all product images share one `src`) and are kept as documented `test.fail()` cases. See [LLM-EXERCISE-NOTES.md](./LLM-EXERCISE-NOTES.md).

18/08/2026 - "AI Safety" topic, exercise 1: asked an AI (blind) for a "secure" `auth/authHelper.js`. It works (see `tests/04-auth-helper-smoke.spec.ts`) but a manual audit against the OWASP Authentication/Password Storage/Session Management Cheat Sheets found 10 real gaps (hardcoded JWT secret fallback, user enumeration, no rate limiting, bcrypt's 72-byte truncation, etc.). See [AI-AUTH-AUDIT.md](./AI-AUTH-AUDIT.md).

18/08/2026 - "AI Safety" topic, exercise 2: went looking for a hallucinated API call. Two blind attempts (`express-rate-limit`, `crypto.hash()`) were correct on the first try. `auth/totp.js` (otplib) was not — it used the `authenticator` object from otplib v10-12, which `otplib@13` (a complete rewrite) removed entirely. Left as-written to document the failure. See [AI-HALLUCINATION-EXAMPLE.md](./AI-HALLUCINATION-EXAMPLE.md).

18/08/2026 - "AI Safety" topic, exercise 3: ran a license scan (`license-checker`, since FOSSA itself needs an account) over the whole project. ~390 of ~408 packages are permissive as expected; flagged `@promptbook/utils` (CC-BY-4.0, a content license applied to code) buried 4 levels deep in `webdriverio`'s transitive dependencies. See [LICENSE-SCAN-NOTES.md](./LICENSE-SCAN-NOTES.md).

18/08/2026 - "Advanced AI Tools" topic, exercise 1: a real multi-file refactor wiring `auth/authHelper.js`, `auth/loginRateLimiter.js`, and a new `auth/authRouter.js` together to actually close the OWASP audit findings instead of leaving them as fixes-on-paper. Running the new integration test caught a real bug mid-review (the rate limiter's counter leaked across test servers because it was a module-level singleton); fixed by converting it to a factory. See [MULTI-FILE-REFACTOR-NOTES.md](./MULTI-FILE-REFACTOR-NOTES.md).

18/08/2026 - "Advanced AI Tools" topic, exercise 3: added [CLAUDE.md](./CLAUDE.md) documenting this repo's real conventions, then verified compliance with a fresh sub-agent (no memory of prior context) given only a small task and told to read CLAUDE.md first. It followed the numbering, chaining, CommonJS, and Spanish-comment rules correctly, and generalized the page-chaining pattern to a case CLAUDE.md doesn't mention by name. Verified independently (re-read the diff, re-ran the full suite) rather than trusting its self-report. See [CLAUDE-MD-VERIFICATION.md](./CLAUDE-MD-VERIFICATION.md).

18/08/2026 - "AI for Test Automation" topic, exercise 1: generated `pages/MenuPage.js` (the hamburger menu — previously uncovered) blind, then audited every locator against the live DOM instead of just confirming they worked. All 6 worked, but 4 of 6 used `id` when the real elements also expose a `data-test` attribute (this repo's actual convention) — corrected. See [MENU-PAGE-AUDIT.md](./MENU-PAGE-AUDIT.md). Exercise 3 (converting manual waits to auto-retrying matchers) was left out — the repo has exactly one manual wait total (`mobile/screens/BaseScreen.js`), and it's a deliberate, already-documented flakiness fix, not a pattern to convert.

18/08/2026 - "AI for Test Automation" topic, exercise 2: generated `testdata/users.json` (50 plausible users for `auth/authHelper.registerUser()`) by combining a 12x12 name pool independently per record — no manual dedup. Spot-check found 3 real duplicate usernames (expected: birthday-paradox math on a small pool), and `tests/08-user-seed-data.spec.ts` confirmed against the real `registerUser()` code that exactly those 3, and no others, fail to register. See [TEST-DATA-AUDIT.md](./TEST-DATA-AUDIT.md).

18/08/2026 - "AI-Assisted Debugging" topic, exercise 1: pasted the real flaky rate-limiter bug (error + pre-fix code + what was already ruled out) into a fresh, context-free agent and asked for ranked hypotheses. Its #1 hypothesis matched the real root cause exactly, and explained the mechanism (success requests count against the limit by default) more completely than the original write-up did. See [FLAKY-BUG-HYPOTHESIS-RANKING.md](./FLAKY-BUG-HYPOTHESIS-RANKING.md).

18/08/2026 - "AI-Assisted Debugging" topic, exercise 2: pasted the real ~135-line failing log from the first (unfixed) run of `tests/03-llm-generated-sort.spec.ts` into a fresh agent and asked for the first interesting line. It correctly picked the first `x` marker over all the passing/boilerplate lines above it, and unprompted distinguished "first sign of trouble" from "first line explaining the mechanism" — but its justification for the second answer included a plausible-sounding but factually wrong causal link between two independent SauceDemo bugs. See [LOG-TRIAGE-EXAMPLE.md](./LOG-TRIAGE-EXAMPLE.md).

18/08/2026 - "AI-Assisted Debugging" topic, exercise 3: ran the otplib bug (same one from AI-HALLUCINATION-EXAMPLE.md) as a real multi-turn chat with a fresh agent, verifying every claim it made by actually running it. Result didn't match the exercise's premise — it reached the correct diagnosis with 0 wrong hypotheses (vs. the 2 wrong guesses made earlier this session going in blind, on the exact same bug). Documented as the more useful finding: the gap between 2 wrong guesses and 0 was the debugging method (guess from memory vs. demand evidence before hypothesizing), not the bug's difficulty. See [DEBUGGING-HYPOTHESIS-COUNT.md](./DEBUGGING-HYPOTHESIS-COUNT.md).

18/08/2026 - Added [EXERCISES-SUMMARY.md](./EXERCISES-SUMMARY.md), an index of every AI-topic exercise delivered (LLM, AI Safety, Advanced AI Tools, AI for Test Automation, AI-Assisted Debugging), what was skipped and why, and the resulting code layout — ahead of the final course deliverable.

18/08/2026 - Final course deliverable: [AI-ASSISTED-EDITS-LOG.md](./AI-ASSISTED-EDITS-LOG.md), 10 real prompt → AI output → final edit → why-changed entries pulled from all of the above (not staged for this doc) — from hardening `authHelper.js` against OWASP to the one case where the correct edit was deliberately *not* touching the file (`auth/totp.js`, kept as hallucination evidence).
